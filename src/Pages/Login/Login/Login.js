import React from 'react';
import PassLogin from '../PassLogin/PassLogin';

const Login = () => {
    return (
        <div>
            <PassLogin></PassLogin>
        </div>
    );
};

export default Login;